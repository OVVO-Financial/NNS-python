name: Native backend CI

on:
  pull_request:
  push:

permissions:
  contents: write
  actions: write

jobs:
  cleanup-obsolete-workflows:
    name: Purge obsolete Actions history
    if: github.event_name == 'push' && github.event.head_commit.message == 'Purge obsolete Actions workflow history'
    runs-on: ubuntu-latest
    steps:
      - name: Delete